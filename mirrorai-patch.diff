{
    "patch": {
        "filename": "user_authentication.py",
        "additions": [
            "@@ -1,3 +1,5 @@",
            " def authenticate(username, password):",
            "     # TODO: Implement user authentication logic here",
            "+    # Prevent SQLi and XSS attacks",
            "+    username = escape_sql(username)",
            "+    password = escape_sql(password)",
            "+    # Check if the user exists in the database",
            "+    user = User.query.filter_by(username=username).first()",
            "+    if not user:",
            "+        return False, 'User does not exist'",
            "+    # Check if the password is correct",
            "+    if not check_password(password, user.password):",
            "+        return False, 'Incorrect password'",
            "+    # Return True and the user object if authentication succeeds",
            "+    return True, user"
        ],
        "deletions": []
    }
}